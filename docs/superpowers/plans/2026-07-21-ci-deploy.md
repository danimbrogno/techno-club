# Techno Club CI Deploy Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship GitHub Actions deploy on merge to `main` that builds changed `plugins/*` and `lessons/*` JARs, rsyncs them to `aztec-validator:/srv/minecraft/plugins`, and restarts via `restart-technoclub.sh`.

**Architecture:** GitHub-hosted runner → Tailscale (`tag:ci-technoclub`) → SSH as `tcdeploy` → rsync + sudo restart. Secrets live in GitHub Environment `dev`.

**Tech Stack:** GitHub Actions, Temurin JDK 21, Gradle wrappers per unit, Tailscale GitHub Action v4, rsync/SSH.

**Spec:** `docs/superpowers/specs/2026-07-21-ci-deploy-design.md` (use Environment `dev`, not `technoclub`).

---

### Task 1: Canonical restart script in repo

**Files:**
- Create: `scripts/remote-restart.sh`

- [ ] Add screen-based restart script matching live `/usr/local/bin/restart-technoclub.sh`
- [ ] Commit

### Task 2: Deploy workflow

**Files:**
- Create: `.github/workflows/deploy.yml`
- Modify: `docs/superpowers/specs/2026-07-21-ci-deploy-design.md` (environment name → `dev`)

- [ ] Workflow: push to `main` + `workflow_dispatch` (`deploy_all`)
- [ ] Detect changed units under `plugins/` and `lessons/`
- [ ] Build with JDK 21, stage JARs, Tailscale, rsync, restart
- [ ] `environment: dev`
- [ ] Commit

### Task 3: First deploy

- [ ] Push to `main` (or `workflow_dispatch`)
- [ ] Watch `gh run watch`; fix failures until green
